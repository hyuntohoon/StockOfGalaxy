export interface RocketData {
  userId: number;
  nickname: string;
  price: string;
  priceChangeSign: string;
  priceChange: string;
  message: string;
  createdAt: string;
  imageUrl: string;
}

// todo: imageUrl -> characterType(number) 로 변경해야 함
